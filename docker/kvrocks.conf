# Kvrocks configuration for bob standalone container
bind 127.0.0.1
port 6666
dir /data/kvrocks
daemonize no

# Workers
workers 4

# Logging
log-dir /app/logs
log-level info

# RocksDB settings
rocksdb.compression zstd
rocksdb.max_open_files 4096
# Block cache size in MB (default 4096MB - too high for most containers)
# Block cache size in MB - uses remaining RAM after Redis (2GB) and OS/app overhead (~2GB)
# For a 32GB host: ~28GB available. For 16GB: ~12GB. Adjust per your host.
rocksdb.block_cache_size 4096
# Write buffer size in MB (default 64MB)
rocksdb.write_buffer_size 256
rocksdb.max_write_buffer_number 4

rocksdb.ttl 1296000

# Compaction scheduler - checks every hour
compaction-checker-cron * * * * *
# Force compact files older than 48 hours with at least 10% deleted records
force-compact-file-age 172800
force-compact-file-min-deleted-percentage 10
